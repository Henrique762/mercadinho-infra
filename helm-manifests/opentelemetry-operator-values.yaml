manager:
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi

nodeSelector:
  kubernetes.io/hostname: minikube-m02

admissionWebhooks:
  certManager:
    enabled: true
